import React from 'react';
import Header from '../Components/Header';

const AuthLayout = () => {
    return (
        <div>
            <Header></Header>
            this is auth layout
        </div>
    );
};

export default AuthLayout;